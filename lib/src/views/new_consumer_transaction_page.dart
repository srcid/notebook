import 'package:flutter/material.dart';

class NewConsumerTransactionPage extends StatelessWidget {
  const NewConsumerTransactionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
